# Contributing

1. Use a feature branch.
2. Never add production secrets/data.
3. Run `bash -n` on changed shell scripts.
4. Run `bin/oracle-native-standby-validate`.
5. Test changed Oracle RMAN/Data Guard paths in non-production.
6. Update CHANGELOG.md.
7. Keep destructive/cutover operations explicit and guarded.
